# External Module Versioning

*Coming soon.*

Future support for tracking and versioning external module dependencies from:

- Terraform Registry
- Git repositories
- S3/GCS buckets

Currently, external modules work through standard Terraform mechanisms but aren't tracked as Bazel dependencies.
