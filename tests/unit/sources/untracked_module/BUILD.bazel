load("//tf2/internal:sources_validation.bzl", "tf_untracked_files_negative_test")

# Negative test: untracked.tf is intentionally NOT in srcs
# This test expects to find untracked files and passes if it does
tf_untracked_files_negative_test(
    name = "untracked_negative_test",
    size = "small",
    srcs = [
        "README.md",
        "main.tf",  # Only track main.tf, intentionally exclude untracked.tf
    ],
    visibility = ["//tests/unit/sources:__pkg__"],
)
