load(":integration_test.bzl", "tflint_autogen_integration_test_suite")

# Integration tests for TFLint auto-generation functionality
# Each test module is in its own subdirectory to avoid conflicts

# Run the integration test suite
tflint_autogen_integration_test_suite(name = "integration_tests")
