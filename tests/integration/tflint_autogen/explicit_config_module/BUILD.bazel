load("//tf2:def.bzl", "tf_module")

# Module with explicit TFLint config (should not auto-generate)
tf_module(
    name = "tf_module",
    testonly = True,
    srcs = ["terraform.tf"],
    providers = ["@tf_provider_registry//:aws_5"],
    visibility = ["//tests/integration/tflint_autogen:__pkg__"],
)
