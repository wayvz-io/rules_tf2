load("//tf2:def.bzl", "tf_module")

# Define the module
tf_module(
    name = "simple_stack",
    srcs = glob(["*.tf"]) + ["README.md"],
    providers = [
        "@tf_provider_registry//:random_3",
        "@tf_provider_registry//:local_2",
        "@tf_provider_registry//:null_3",
    ],
    tfdoc_config = "//tests/fixtures:tfdoc-config",
    tflint_config = "//tests/fixtures:tflint-config-stacks",
    visibility = ["//visibility:public"],
)
