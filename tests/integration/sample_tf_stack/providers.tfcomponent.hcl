# Provider configurations for the sample stack

provider "random" "main" {
  # No configuration needed for random provider
}

provider "local" "main" {
  # No configuration needed for local provider
}

provider "null" "main" {
  # No configuration needed for null provider
}
