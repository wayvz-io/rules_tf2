load("//tf2:def.bzl", "tf_module")

# Test module to verify HCL-based version management
tf_module(
    name = "hcl_versions_test",
    srcs = glob(["*.tf"]),
    providers = [
        "@tf_provider_registry//:random_3",
        "@tf_provider_registry//:local_2",
    ],
)
